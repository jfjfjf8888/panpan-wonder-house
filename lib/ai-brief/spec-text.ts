export const GAME_AI_SPEC = '你现在需要为网站 **《盼盼与熊大的妙妙屋》** 制作一个可上传、可独立运行的 HTML5 小游戏。\n\n请严格按照以下规范开发和打包。最终必须输出一个完整 ZIP 游戏包。这个 ZIP 包会被上传到网站后台并执行自动格式检查，不符合要求的包无法发布。\n\n# 一、本次游戏需求\n\n游戏名称：\n\n```text\n【填写游戏名称】\n```\n\n游戏创意和玩法：\n\n```text\n【填写具体玩法、目标、规则和脑洞】\n```\n\n特殊要求：\n\n```text\n【没有则填写“无”】\n```\n\n请先根据以上需求完成游戏设计，然后直接开发完整游戏，不要只输出设计方案或零散代码。\n\n---\n\n# 二、技术要求\n\n游戏必须是纯前端 HTML5 游戏。\n\n允许使用：\n\n* HTML。\n* CSS。\n* 原生 JavaScript。\n* Canvas 2D。\n* Web Audio API。\n* 已经完整放入游戏包内的本地前端库。\n\n禁止使用：\n\n* 服务端程序。\n* Node.js 运行环境。\n* PHP、Java、Python 等后端代码。\n* 数据库。\n* 外部 CDN。\n* 外部 JavaScript。\n* 外部 CSS。\n* 外部图片地址。\n* 外部字体地址。\n* 外部音频地址。\n* 第三方统计 SDK。\n* 第三方广告 SDK。\n* 外部 iframe。\n* `eval()`。\n* `new Function()`。\n* 明显混淆或加密的 JavaScript。\n* 必须联网才能运行的功能。\n\n游戏解压后必须能够直接从 `index.html` 启动。\n\n所有文件路径必须使用相对路径。\n\n正确：\n\n```text\n./assets/images/player.webp\n./js/game.js\n```\n\n错误：\n\n```text\n/assets/images/player.webp\nhttps://example.com/player.webp\n```\n\n---\n\n# 三、设备适配要求\n\n游戏必须同时支持：\n\n* Android 手机。\n* iPhone。\n* 平板。\n* Windows 电脑。\n* macOS 电脑。\n* 触摸操作。\n* 鼠标操作。\n\n存在键盘玩法时，同时支持键盘，但不能只支持键盘。\n\n页面必须包含：\n\n```html\n<meta\n  name="viewport"\n  content="width=device-width, initial-scale=1, viewport-fit=cover"\n/>\n```\n\n游戏页面要求：\n\n* 最小支持 320px 宽度。\n* 不产生横向滚动条。\n* 不依赖鼠标悬停。\n* 按钮在手机上容易点击。\n* 文字不能小到无法阅读。\n* 画布根据屏幕大小缩放。\n* 不同屏幕比例下主要内容不能被裁掉。\n* 支持手机安全区域。\n* 页面失去焦点时自动暂停。\n* 页面重新获得焦点后可以继续。\n* 横竖屏不支持时显示清楚的旋转提示。\n\n---\n\n# 四、声音要求\n\n游戏可以有背景音乐和音效，但必须满足：\n\n* 页面打开后不得立即自动播放声音。\n* 必须在玩家第一次点击或触摸后才能播放声音。\n* 必须提供静音能力。\n* 游戏暂停后停止或暂停声音。\n* 页面隐藏后暂停声音。\n* 游戏结束后不得让音频继续失控播放。\n\n所有音频文件必须包含在 ZIP 包中。\n\n---\n\n# 五、游戏包目录要求\n\nZIP 包根目录必须直接包含以下文件，不允许外面再套一层文件夹。\n\n标准结构：\n\n```text\n/\n├── manifest.json\n├── index.html\n├── cover.webp\n├── icon.png\n├── README.md\n├── css/\n│   └── game.css\n├── js/\n│   ├── game.js\n│   └── game-bridge.js\n├── assets/\n│   ├── images/\n│   ├── audio/\n│   └── fonts/\n└── screenshots/\n    ├── 01.webp\n    └── 02.webp\n```\n\n必须包含：\n\n```text\nmanifest.json\nindex.html\ncover.webp\njs/game-bridge.js\n```\n\n建议包含：\n\n```text\nicon.png\nREADME.md\nscreenshots/\n```\n\nZIP 文件名格式：\n\n```text\ngame-{slug}-v{version}.zip\n```\n\n示例：\n\n```text\ngame-memory-card-v1.0.0.zip\n```\n\n---\n\n# 六、文件大小要求\n\n* ZIP 包不超过 100 MB。\n* 解压后不超过 300 MB。\n* 单个文件不超过 30 MB。\n* 文件数量不超过 2,000 个。\n* `cover.webp` 不超过 2 MB。\n* 截图最多 6 张。\n* 尽量压缩图片和音频。\n* 删除没有使用的素材。\n\n---\n\n# 七、封面及图标要求\n\n## cover.webp\n\n必须提供游戏封面：\n\n* 文件名固定为 `cover.webp`。\n* 推荐尺寸为 1200×900。\n* 宽高比为 4:3。\n* 封面中包含能够代表玩法的主要画面。\n* 可以包含游戏标题。\n* 不要放过多小字。\n* 不得使用存在版权问题的角色或素材。\n\n## icon.png\n\n建议提供：\n\n* 文件名为 `icon.png`。\n* 推荐尺寸为 512×512。\n* PNG 格式。\n* 图形简洁。\n* 在小尺寸下仍容易识别。\n\n所有美术素材必须原创、可合法使用或由 AI 生成，不得直接复制影视、动漫、游戏或其他网站素材。\n\n---\n\n# 八、manifest.json 要求\n\n必须按照以下格式生成：\n\n```json\n{\n  "schemaVersion": "1.0",\n  "id": "game-slug",\n  "slug": "game-slug",\n  "title": "游戏中文名称",\n  "shortDescription": "不超过60字的一句话游戏介绍。",\n  "description": "不超过500字的完整游戏介绍。",\n  "version": "1.0.0",\n  "author": "PanPan Lab",\n  "entry": "index.html",\n  "cover": "cover.webp",\n  "icon": "icon.png",\n  "tags": ["休闲", "益智"],\n  "orientation": "any",\n  "controls": ["touch", "mouse", "keyboard"],\n  "display": {\n    "aspectRatio": "16:9",\n    "minWidth": 320,\n    "maxWidth": 1920,\n    "supportsFullscreen": true\n  },\n  "permissions": {\n    "audio": true,\n    "storage": true,\n    "fullscreen": true,\n    "networkDomains": []\n  },\n  "analytics": {\n    "enabled": true\n  },\n  "adCapabilities": {\n    "banner": false,\n    "interstitial": true,\n    "rewarded": true\n  }\n}\n```\n\n字段规则：\n\n* `id`：使用小写英文、数字和短横线，发布后永久不变。\n* `slug`：通常与 id 相同。\n* `title`：2 至 30 个字符。\n* `shortDescription`：最多 60 个字符。\n* `description`：最多 500 个字符。\n* `version`：使用 `1.0.0` 形式。\n* `entry`：必须指向真实存在的入口文件。\n* `cover`：必须指向真实存在的 WebP 文件。\n* `tags`：最多 5 个。\n* `orientation`：只能是 `portrait`、`landscape` 或 `any`。\n* `controls`：只填写游戏实际支持的操作方式。\n* `networkDomains`：默认必须为空数组。\n* `adCapabilities`：只是预留能力，当前网站不会真正播放广告。\n\n制作新游戏时版本号使用：\n\n```text\n1.0.0\n```\n\n重要：每次重新打包并准备上传后台时，都必须升高 `manifest.json` 的 `version`，同时同步修改 ZIP 文件名中的版本号。版本不升级就上传，容易与后台已有版本冲突（同版本覆盖警告，或低于线上版本被拒绝）。即使只是修一个小问题，也至少把修订号加一，例如 `1.0.0` → `1.0.1`。\n\n更新已有游戏时：\n\n* id 不得改变。\n* slug 不得改变。\n* 每次打包都必须提高版本号，不得沿用旧版本号。\n* 修复问题可以从 `1.0.0` 更新到 `1.0.1`。\n* 增加功能可以更新到 `1.1.0`。\n* 大规模改版可以更新到 `2.0.0`。\n\n---\n\n# 九、网站通信协议\n\n游戏运行在网站 iframe 内，必须通过 `window.parent.postMessage()` 与网站通信。\n\n所有游戏发送的消息必须使用以下公共结构：\n\n```javascript\n{\n  source: "panpan-game",\n  version: "1.0",\n  type: "事件名称",\n  payload: {}\n}\n```\n\n发送消息时使用：\n\n```javascript\nwindow.parent.postMessage(\n  {\n    source: "panpan-game",\n    version: "1.0",\n    type: "GAME_READY",\n    payload: {}\n  },\n  "*"\n);\n```\n\n必须支持以下事件。\n\n## 1. GAME_READY\n\n游戏资源完成加载，并且可以开始操作时发送一次：\n\n```javascript\nsendToHost("GAME_READY", {\n  gameVersion: "1.0.0"\n});\n```\n\n## 2. GAME_START\n\n玩家正式开始一局时发送：\n\n```javascript\nsendToHost("GAME_START", {\n  mode: "normal"\n});\n```\n\n不要在网页刚加载时发送，必须在玩家真正开始游戏时发送。\n\n## 3. GAME_PAUSE\n\n游戏暂停时发送：\n\n```javascript\nsendToHost("GAME_PAUSE", {});\n```\n\n## 4. GAME_RESUME\n\n游戏恢复时发送：\n\n```javascript\nsendToHost("GAME_RESUME", {});\n```\n\n## 5. SCORE_UPDATE\n\n分数发生重要变化时发送：\n\n```javascript\nsendToHost("SCORE_UPDATE", {\n  score: 300\n});\n```\n\n不要每一帧发送。\n\n## 6. LEVEL_COMPLETE\n\n完成关卡时发送：\n\n```javascript\nsendToHost("LEVEL_COMPLETE", {\n  level: 3,\n  score: 600\n});\n```\n\n## 7. GAME_END\n\n一局结束时必须发送：\n\n```javascript\nsendToHost("GAME_END", {\n  score: 850,\n  durationSeconds: 92,\n  result: "completed"\n});\n```\n\n`result` 可以使用：\n\n```text\ncompleted\nfailed\nquit\ntimeout\n```\n\n## 8. GAME_ERROR\n\n发生无法正常继续的错误时发送：\n\n```javascript\nsendToHost("GAME_ERROR", {\n  code: "RESOURCE_LOAD_FAILED",\n  message: "背景图片加载失败",\n  fatal: true\n});\n```\n\n---\n\n# 十、game-bridge.js 要求\n\n必须创建 `js/game-bridge.js`，集中封装网站通信，不要在游戏各处散落大量 `postMessage` 代码。\n\n至少实现：\n\n```javascript\n(function () {\n  "use strict";\n\n  const HOST_SOURCE = "panpan-host";\n  const GAME_SOURCE = "panpan-game";\n  const PROTOCOL_VERSION = "1.0";\n\n  let hostConfig = {\n    muted: false,\n    adsEnabled: false\n  };\n\n  function send(type, payload) {\n    window.parent.postMessage(\n      {\n        source: GAME_SOURCE,\n        version: PROTOCOL_VERSION,\n        type,\n        payload: payload || {}\n      },\n      "*"\n    );\n  }\n\n  function onHostMessage(event) {\n    const message = event.data;\n\n    if (!message || message.source !== HOST_SOURCE) {\n      return;\n    }\n\n    if (message.version !== PROTOCOL_VERSION) {\n      return;\n    }\n\n    switch (message.type) {\n      case "HOST_INIT":\n        hostConfig = {\n          ...hostConfig,\n          ...message.payload\n        };\n\n        window.dispatchEvent(\n          new CustomEvent("panpan:host-init", {\n            detail: hostConfig\n          })\n        );\n        break;\n\n      case "HOST_PAUSE":\n        window.dispatchEvent(new CustomEvent("panpan:pause"));\n        break;\n\n      case "HOST_RESUME":\n        window.dispatchEvent(new CustomEvent("panpan:resume"));\n        break;\n\n      case "AUDIO_CHANGE":\n        hostConfig.muted = Boolean(message.payload?.muted);\n\n        window.dispatchEvent(\n          new CustomEvent("panpan:audio-change", {\n            detail: {\n              muted: hostConfig.muted\n            }\n          })\n        );\n        break;\n\n      case "AD_RESULT":\n        window.dispatchEvent(\n          new CustomEvent("panpan:ad-result", {\n            detail: message.payload || {}\n          })\n        );\n        break;\n    }\n  }\n\n  window.addEventListener("message", onHostMessage);\n\n  window.PanPanGame = {\n    send,\n    ready(payload) {\n      send("GAME_READY", payload);\n    },\n    start(payload) {\n      send("GAME_START", payload);\n    },\n    pause(payload) {\n      send("GAME_PAUSE", payload);\n    },\n    resume(payload) {\n      send("GAME_RESUME", payload);\n    },\n    score(payload) {\n      send("SCORE_UPDATE", payload);\n    },\n    levelComplete(payload) {\n      send("LEVEL_COMPLETE", payload);\n    },\n    end(payload) {\n      send("GAME_END", payload);\n    },\n    error(payload) {\n      send("GAME_ERROR", payload);\n    },\n    requestAd(payload) {\n      send("AD_REQUEST", payload);\n    },\n    getHostConfig() {\n      return { ...hostConfig };\n    }\n  };\n})();\n```\n\n`index.html` 必须在游戏主脚本之前引入：\n\n```html\n<script src="./js/game-bridge.js"></script>\n<script src="./js/game.js"></script>\n```\n\n---\n\n# 十一、广告兼容要求\n\n当前网站不会实际接入广告，但游戏需要预留兼容能力。\n\n游戏可以请求：\n\n* 插屏广告 `interstitial`。\n* 激励广告 `rewarded`。\n\n请求示例：\n\n```javascript\nwindow.PanPanGame.requestAd({\n  format: "rewarded",\n  placement: "continue_after_failure",\n  requestId: "rewarded-001"\n});\n```\n\n网站可能返回：\n\n```text\ncompleted\nskipped\nno_fill\nerror\ndisabled\n```\n\n只有收到：\n\n```text\ncompleted\n```\n\n时才发放激励奖励。\n\n收到以下结果时，游戏必须正常继续，不能卡死：\n\n```text\ndisabled\nno_fill\nerror\nskipped\n```\n\n当前广告默认关闭，因此游戏必须完整支持无广告运行。\n\n---\n\n# 十二、本地存档要求\n\n需要保存设置、关卡或最高分时，可以使用 localStorage。\n\n所有键必须增加游戏命名空间：\n\n```text\npanpan_game_{gameId}_{key}\n```\n\n示例：\n\n```javascript\nlocalStorage.setItem(\n  "panpan_game_memory-card_high-score",\n  "1000"\n);\n```\n\n不得使用过于通用的名称，例如：\n\n```text\nscore\nsettings\ndata\n```\n\n读取损坏或不存在的存档时必须使用默认值，不能导致游戏无法启动。\n\n---\n\n# 十三、错误处理要求\n\n游戏必须处理：\n\n* 图片加载失败。\n* 音频加载失败。\n* 存档内容损坏。\n* 浏览器不支持某个 API。\n* Canvas 初始化失败。\n* 未捕获 JavaScript 异常。\n* 手机横竖屏变化。\n* 页面切换到后台。\n* 用户快速重复点击。\n\n至少监听：\n\n```javascript\nwindow.addEventListener("error", function (event) {\n  window.PanPanGame?.error({\n    code: "UNHANDLED_ERROR",\n    message: event.message || "未知错误",\n    fatal: false\n  });\n});\n\nwindow.addEventListener("unhandledrejection", function (event) {\n  window.PanPanGame?.error({\n    code: "UNHANDLED_PROMISE_REJECTION",\n    message: String(event.reason || "未知异步错误"),\n    fatal: false\n  });\n});\n```\n\n---\n\n# 十四、README.md 要求\n\nREADME 必须说明：\n\n```text\n1. 游戏名称\n2. 游戏玩法\n3. 操作方式\n4. 支持的设备\n5. 推荐屏幕方向\n6. 游戏入口文件\n7. 当前版本\n8. 游戏事件说明\n9. 本地存档键名\n10. 使用的第三方代码或素材\n11. 已知问题\n12. 本地测试方法\n```\n\n不存在第三方代码或素材时明确写：\n\n```text\n本游戏未使用第三方代码或外部素材。\n```\n\n---\n\n# 十五、禁止文件\n\nZIP 包中不得出现：\n\n```text\n.exe\n.dll\n.so\n.dylib\n.apk\n.ipa\n.php\n.jsp\n.asp\n.aspx\n.sh\n.bat\n.cmd\n.ps1\n.py\n.jar\n```\n\n同时不得包含：\n\n* `.git`。\n* `node_modules`。\n* 源项目构建缓存。\n* 操作系统临时文件。\n* 编辑器缓存。\n* 密钥。\n* Token。\n* 私有接口地址。\n* 无关素材。\n* 未使用的大文件。\n\n---\n\n# 十六、完成前自检\n\n在输出 ZIP 前，必须逐项检查：\n\n* ZIP 根目录是否直接包含 `index.html`。\n* ZIP 根目录是否直接包含 `manifest.json`。\n* ZIP 根目录是否直接包含 `cover.webp`。\n* 是否存在 `js/game-bridge.js`。\n* manifest 是否是合法 JSON。\n* manifest 中的所有路径是否真实存在。\n* id 与 slug 是否符合命名规则。\n* 版本号是否正确，且相对上次上传已升级。\n* 所有资源是否使用相对路径。\n* 是否完全移除了外部 CDN。\n* 断网情况下是否能够启动。\n* 是否能够发送 `GAME_READY`。\n* 是否能够发送 `GAME_START`。\n* 是否能够发送 `GAME_END`。\n* 是否能够上报错误。\n* 手机触摸是否可操作。\n* 电脑鼠标是否可操作。\n* 320px 宽度是否正常。\n* 页面是否没有横向滚动。\n* 声音是否不会自动播放。\n* 静音是否生效。\n* 页面隐藏后是否暂停。\n* 广告 disabled 时是否能够继续。\n* 封面是否能够正常显示。\n* 控制台是否没有阻断性错误。\n* ZIP 是否没有多余的外层目录。\n\n---\n\n# 十七、最终交付要求\n\n最终必须交付：\n\n1. 一个完整可运行的 ZIP 游戏包。\n2. ZIP 文件名符合规定。\n3. 一份 ZIP 内文件清单。\n4. 一份 manifest 内容。\n5. 一份自检结果。\n6. 一段游戏玩法简介。\n7. 一段后台展示用的一句话介绍。\n8. 游戏操作说明。\n9. 已知问题说明。\n10. 更新版本时提供本次更新内容。\n\n不得只提供代码片段。\n\n不得只提供单个 HTML 文件。\n\n不得让我自行创建目录、补文件或重新打包。\n\n不得声称已经测试但实际上没有运行测试。\n\n最终 ZIP 解压后必须能够直接运行，并可以上传到《盼盼与熊大的妙妙屋》后台通过格式检查。\n' as string;
